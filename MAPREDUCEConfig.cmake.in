# - Config file for the FooBar package
# It defines the following variables
#  MAPREDUCE_INCLUDE_DIRS - include directories for MAPREDUCE
#  MAPREDUCE_LIBRARIES    - libraries to link against
#  TSDF2MESH_MAPREDUCE_EXECUTABLE   - the TSDF2MESH executable
#  INTEGRATE_MAPREDUCE_EXECUTABLE   - the integrate executable

# Find and Set the library
#FIND_LIBRARY(MAPREDUCE_LIBRARY mapreduce
#    PATHS @CMAKE_INSTALL_PREFIX@/lib
#    NO_DEFAULT_PATH
#)
#SET(MAPREDUCE_LIBRARIES ${MAPREDUCE_LIBRARY})
find_package(Boost COMPONENTS exception filesystem system iostreams)
SET(MAPREDUCE_LIBRARIES ${Boost_EXCEPTION_LIBRARY} ${Boost_FILESYSTEM_LIBRARY} ${Boost_SYSTEM_LIBRARY} ${Boost_IOSTREAMS_LIBRARY})

# Find and set the include dirs
SET(MAPREDUCE_INCLUDE_DIR "@CMAKE_INSTALL_PREFIX@/include/mapreduce")

SET(MAPREDUCE_INCLUDE_DIRS ${MAPREDUCE_INCLUDE_DIR})

# Find and set the executables
#SET(TSDF2MESH_MAPREDUCE_EXECUTABLE "@CMAKE_INSTALL_PREFIX@/bin/tsdf2mesh")
#SET(INTEGRATE_MAPREDUCE_EXECUTABLE "@CMAKE_INSTALL_PREFIX@/bin/integrate")
